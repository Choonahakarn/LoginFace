# ตั้งค่า SMTP ให้ใช้งานได้จริง (Step-by-Step)

## 🎯 เป้าหมาย

ตั้งค่า Custom SMTP (Resend) ให้ส่ง email ได้จริง เพื่อให้:
- ✅ Signup สำเร็จ
- ✅ ส่ง email verification ได้
- ✅ Login ได้หลัง verify email

## 📋 ขั้นตอนที่ 1: สมัครและตั้งค่า Resend

### 1.1 สมัคร Resend

1. **ไปที่**: https://resend.com
2. **คลิก**: "Get Started" หรือ "Sign Up"
3. **เลือกวิธีสมัคร**:
   - Google Account (แนะนำ - เร็วที่สุด)
   - GitHub Account
   - Email/Password
4. **ยืนยันอีเมล** (ถ้าสมัครด้วย email)

### 1.2 สร้าง API Key

1. **เข้าสู่ระบบ Resend Dashboard**
2. **ไปที่**: Dashboard → **API Keys** (เมนูด้านซ้าย)
3. **คลิก**: "Create API Key"
4. **ตั้งค่า**:
   - **Name**: `Supabase SMTP` (หรือชื่ออื่น)
   - **Permission**: `Sending access` (หรือ Full access)
5. **คลิก**: "Add"
6. **Copy API Key**: ⚠️ **สำคัญ!** Copy ทันทีเพราะจะแสดงแค่ครั้งเดียว
   - ตัวอย่าง: `re_1234567890abcdefghijklmnopqrstuvwxyz`

## 📋 ขั้นตอนที่ 2: ตั้งค่า SMTP ใน Supabase

### 2.1 ไปที่ SMTP Settings

1. **Supabase Dashboard** → **Authentication** → **Email** → **SMTP Settings**
   (หรือ Authentication → SMTP Settings โดยตรง)

### 2.2 กรอก Sender Details

ในส่วน **"Sender details"**:

- **Sender email address**: 
  ```
  onboarding@resend.dev
  ```
  (ใช้ชั่วคราว - Resend ให้ใช้ได้เลย ไม่ต้อง verify)

- **Sender name**: 
  ```
  Face Attendance System
  ```
  (หรือชื่อที่ต้องการ)

### 2.3 กรอก SMTP Provider Settings

ในส่วน **"SMTP provider settings"**:

- **Host**: 
  ```
  smtp.resend.com
  ```
  (ตรวจสอบว่าพิมพ์ถูกต้อง ไม่มี space)

- **Port number**: 
  ```
  587
  ```
  (ไม่ใช่ 465 - ใช้ 587 สำหรับ TLS)

- **Minimum interval per user**: 
  ```
  60
  ```
  (ไม่ต้องเปลี่ยน - 60 วินาที)

- **Username**: 
  ```
  resend
  ```
  (ใช้คำว่า "resend" เท่านั้น - ตัวพิมพ์เล็ก)

- **Password**: 
  ```
  [ใส่ Resend API Key ที่ copy มา]
  ```
  ⚠️ **สำคัญ**: 
  - ใส่ API Key ที่ copy จาก Resend Dashboard
  - Copy มาเต็ม ไม่มี space ข้างหน้า/หลัง
  - ตรวจสอบว่าพิมพ์ถูกต้อง

### 2.4 เปิดใช้งาน Custom SMTP

1. **ตรวจสอบ**: ว่ากรอกทุก field แล้ว
2. **คลิก**: Toggle **"Enable custom SMTP"** (ด้านบน)
   - Toggle จะเปลี่ยนเป็นสีเขียว/เปิด
3. **คลิก**: **"Save changes"** (ปุ่มเขียวด้านล่างขวา)

## 📋 ขั้นตอนที่ 3: ทดสอบ SMTP Connection

### 3.1 ทดสอบใน Supabase Dashboard

1. **ใน SMTP Settings page** หาปุ่ม **"Test email"** หรือ **"Send test email"**
2. **คลิกเพื่อทดสอบ**
3. **ตรวจสอบว่า:**
   - ไม่มี error
   - Email ถูกส่งสำเร็จ

### 3.2 ตรวจสอบ Resend Dashboard

1. **ไปที่**: https://resend.com → Dashboard
2. **ดู Logs** หรือ **Emails**
3. **ตรวจสอบว่า:**
   - มี email ถูกส่งหรือไม่
   - ไม่มี error

## 📋 ขั้นตอนที่ 4: เปิด Email Verification

### 4.1 เปิด Email Verification

1. **Supabase Dashboard** → **Authentication** → **Settings**
2. **หาส่วน "Email"** หรือ **"Email Auth"**
3. **เปิด toggle** **"Enable email confirmations"**
4. **Save**

### 4.2 เปิด Email Template

1. **Supabase Dashboard** → **Authentication** → **Email** → **Templates**
2. **คลิกที่ "Confirm sign up"**
3. **เปิด toggle** **"Enable email"** (ถ้ายังปิดอยู่)
4. **Save changes**

## 📋 ขั้นตอนที่ 5: ทดสอบระบบ

### 5.1 ทดสอบ Signup

1. **เปิดเว็บแอป**: `http://localhost:5173`
2. **ไปที่หน้า Sign Up**
3. **กรอกข้อมูล**:
   - อีเมลใหม่ (ที่ยังไม่เคยใช้)
   - รหัสผ่าน
   - ยืนยันรหัสผ่าน
4. **คลิก "สมัครสมาชิก"**

**ควรเห็น:**
- ✅ Signup สำเร็จ
- ✅ ได้รับ email confirmation
- ✅ ไม่มี error "Error sending confirmation email"

### 5.2 ตรวจสอบ Email

1. **ตรวจสอบ inbox** (และ spam folder)
2. **คลิก confirmation link** ใน email
3. **ตรวจสอบว่า email verified แล้ว**

### 5.3 ทดสอบ Login

1. **ลอง Login** ด้วยอีเมลและรหัสผ่านที่ signup
2. **ตรวจสอบว่า Login สำเร็จ**

## 🆘 ถ้าเจอปัญหา

### Error: "Invalid credentials"

**ตรวจสอบ:**
- Username เป็น `resend` (ตัวพิมพ์เล็ก)
- Password เป็น Resend API Key ที่ถูกต้อง
- Copy API Key มาเต็ม ไม่มี space

**วิธีแก้:**
- ตรวจสอบ Resend Dashboard → API Keys
- Copy API Key ใหม่
- ใส่ใน Password field อีกครั้ง

### Error: "Connection failed"

**ตรวจสอบ:**
- Host เป็น `smtp.resend.com` (ไม่ใช่ `smtp.resend.com `)
- Port เป็น `587` (ไม่ใช่ 465)

**วิธีแก้:**
- ตรวจสอบว่าพิมพ์ถูกต้อง
- ลองเปลี่ยน Port เป็น `465` (ถ้า 587 ไม่ได้)

### Error: "Error sending confirmation email" (500)

**ตรวจสอบ:**
- SMTP Settings ถูกต้อง
- Resend API Key ยังใช้งานได้
- Resend Dashboard → Logs ว่ามี error หรือไม่

**วิธีแก้:**
- ตรวจสอบ Resend Dashboard → Logs
- ลองสร้าง Resend API Key ใหม่
- ทดสอบส่ง test email ใน Supabase Dashboard

### Email ไม่ถูกส่ง

**ตรวจสอบ:**
- Resend Dashboard → Logs
- Supabase Dashboard → Audit Logs
- Spam folder

**วิธีแก้:**
- ตรวจสอบ Resend Dashboard → Logs
- ตรวจสอบว่า API Key ยังใช้งานได้
- ลองส่ง test email

## 📋 Checklist

- [ ] สมัคร Resend สำเร็จ
- [ ] สร้าง API Key และ copy มาแล้ว
- [ ] กรอก Sender email: `onboarding@resend.dev`
- [ ] กรอก Sender name: `Face Attendance System`
- [ ] กรอก Host: `smtp.resend.com`
- [ ] กรอก Port: `587`
- [ ] กรอก Username: `resend`
- [ ] กรอก Password: [Resend API Key]
- [ ] เปิด "Enable custom SMTP"
- [ ] คลิก "Save changes"
- [ ] ทดสอบส่ง test email (ถ้ามี)
- [ ] ตรวจสอบ Resend Dashboard → Logs
- [ ] เปิด Email Verification ใน Settings
- [ ] เปิด Email Template "Confirm sign up"
- [ ] ทดสอบ Signup
- [ ] ตรวจสอบ email inbox
- [ ] ทดสอบ Login

## 💡 คำแนะนำ

- **ใช้ `onboarding@resend.dev` ชั่วคราว**: ไม่ต้อง verify domain
- **Verify Domain ภายหลัง**: ถ้าต้องการใช้ email จาก domain ของคุณเอง
- **Monitor Usage**: ตรวจสอบ email usage ใน Resend Dashboard
- **Test Email First**: ทดสอบส่ง test email ก่อน signup จริง

## 📚 ข้อมูลเพิ่มเติม

- **Resend Free Tier**: 3,000 emails/เดือน
- **Resend Documentation**: https://resend.com/docs
- **Supabase SMTP Docs**: https://supabase.com/docs/guides/auth/auth-smtp
