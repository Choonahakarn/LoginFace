# ตั้งค่าเพื่อใช้ Backend (PyTorch + RetinaFace + InsightFace)
# เมื่อตั้งค่า Frontend จะใช้ MediaPipe สำหรับ detection + liveness
# และส่งภาพไป Backend สำหรับ face recognition
VITE_API_URL=http://localhost:8000
