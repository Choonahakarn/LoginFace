# ขั้นตอนต่อไปหลังจากรัน SQL Schema แล้ว

## ✅ สิ่งที่ทำเสร็จแล้ว
- [x] สร้าง Supabase Project
- [x] รัน SQL Schema

## 📋 ขั้นตอนที่ต้องทำต่อ

### 1. ตั้งค่า Authentication Providers

#### A. Email/Password (ต้องเปิดใช้งาน)
1. ไปที่ Supabase Dashboard → **Authentication** → **Providers**
2. คลิก **Email**
3. ตรวจสอบว่าเปิดใช้งานอยู่ (Enable Email provider)
4. (Optional) ตั้งค่า Email Verification:
   - เปิด **Confirm email** ถ้าต้องการให้ยืนยันอีเมลก่อนใช้งาน
   - ปิด **Confirm email** ถ้าต้องการให้ใช้งานได้ทันที (สำหรับทดสอบ)

#### B. Line OAuth (ถ้าต้องการใช้)
**หมายเหตุ**: Supabase อาจไม่รองรับ Line โดยตรง

**วิธีที่ 1: ใช้ Custom OAuth (ซับซ้อน)**
- ต้องสร้าง Custom Provider ใน Supabase
- ต้องใช้ Backend เพื่อจัดการ OAuth flow

**วิธีที่ 2: ใช้ Email/Password เท่านั้น (แนะนำ)**
- ใช้ Email/Password สำหรับตอนนี้
- เพิ่ม Line ในภายหลังถ้าต้องการ

### 2. ตั้งค่า Environment Variables

#### ขั้นตอน:
1. ไปที่ Supabase Dashboard → **Settings** → **API**
2. Copy **Project URL** (เช่น `https://xxxxx.supabase.co`)
3. Copy **anon public** key (ไม่ใช่ service_role key!)

4. สร้างไฟล์ `.env` ในโฟลเดอร์ `app/`:
   ```bash
   cd app
   # Windows PowerShell
   Copy-Item .env.example .env
   
   # หรือสร้างไฟล์ใหม่
   ```

5. แก้ไขไฟล์ `app/.env`:
   ```env
   VITE_SUPABASE_URL=https://your-project-id.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```

**ตัวอย่าง:**
```env
VITE_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYzODk2NzI5MCwiZXhwIjoxOTU0NTQzMjkwfQ.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### 3. ตั้งค่า Redirect URLs

1. ไปที่ Supabase Dashboard → **Authentication** → **URL Configuration**
2. ในส่วน **Redirect URLs** เพิ่ม:
   ```
   http://localhost:5173/auth/callback
   http://localhost:5173/*
   ```
3. (Optional) ถ้ามี domain จริง เพิ่ม:
   ```
   https://your-domain.com/auth/callback
   https://your-domain.com/*
   ```
4. คลิก **Save**

### 4. ทดสอบระบบ

#### A. รัน Frontend
```bash
cd app
npm install  # ถ้ายังไม่ได้ติดตั้ง dependencies
npm run dev
```

#### B. ทดสอบในเบราว์เซอร์
1. เปิดเบราว์เซอร์ไปที่ `http://localhost:5173`
2. ควรเห็นหน้า Login
3. ทดสอบ:
   - **สมัครสมาชิก**: คลิก "สมัครสมาชิก" → กรอกข้อมูล → สมัคร
   - **Login**: กรอก Email/Password → Login
   - **Logout**: คลิกปุ่ม Logout ที่มุมขวาบน

#### C. ตรวจสอบใน Supabase Dashboard
1. ไปที่ **Authentication** → **Users**
2. ควรเห็น user ที่สมัครใหม่
3. ไปที่ **Table Editor** → **user_profiles**
4. ควรเห็นข้อมูล user profile ที่สร้างอัตโนมัติ

### 5. ตรวจสอบ Database Tables

ไปที่ Supabase Dashboard → **Table Editor** ตรวจสอบว่ามีตารางเหล่านี้:
- ✅ `user_profiles`
- ✅ `classrooms`
- ✅ `students`
- ✅ `student_classrooms`
- ✅ `attendance`
- ✅ `face_embeddings`

## 🔍 Checklist

- [ ] ตั้งค่า Email Provider แล้ว
- [ ] สร้างไฟล์ `.env` และใส่ Supabase URL และ Anon Key
- [ ] ตั้งค่า Redirect URLs แล้ว
- [ ] รัน `npm run dev` และทดสอบ Login/SignUp
- [ ] ตรวจสอบว่า user ถูกสร้างใน Supabase Dashboard
- [ ] ตรวจสอบว่า user_profiles ถูกสร้างอัตโนมัติ

## ⚠️ ปัญหาที่อาจพบ

### Error: Missing Supabase environment variables
**สาเหตุ**: ไฟล์ `.env` ไม่มีหรือตั้งค่าผิด
**แก้ไข**: 
- ตรวจสอบว่าไฟล์ `app/.env` มีอยู่
- ตรวจสอบว่า environment variables ขึ้นต้นด้วย `VITE_`
- Restart dev server หลังจากแก้ไข `.env`

### Error: Invalid API key
**สาเหตุ**: ใช้ Service Role Key แทน Anon Key
**แก้ไข**: 
- ใช้ **anon public** key เท่านั้น (ไม่ใช่ service_role)
- ตรวจสอบว่า copy key มาเต็ม (ไม่มี space)

### Login ไม่ได้
**สาเหตุ**: Email Provider ไม่เปิด หรือ Redirect URL ไม่ถูกต้อง
**แก้ไข**:
- ตรวจสอบว่า Email Provider เปิดอยู่
- ตรวจสอบว่า Redirect URLs ตั้งค่าถูกต้อง

### User Profile ไม่ถูกสร้าง
**สาเหตุ**: Trigger function ไม่ทำงาน
**แก้ไข**:
- ตรวจสอบว่า SQL schema รันเสร็จแล้ว
- ตรวจสอบว่า function `handle_new_user` ถูกสร้างแล้ว

## 📝 หมายเหตุ

- **Line OAuth**: ตอนนี้ยังไม่รองรับ ใช้ Email/Password ก่อน
- **Email Verification**: ถ้าเปิดไว้ ต้องยืนยันอีเมลก่อนใช้งาน
- **Password Policy**: แนะนำให้ตั้งค่ารหัสผ่านขั้นต่ำ 8 ตัวอักษร

## 🎯 ขั้นตอนถัดไป

หลังจากตั้งค่าทุกอย่างเสร็จแล้ว:
1. ทดสอบ Login/SignUp
2. Migrate ข้อมูลจาก localStorage ไป Supabase (ถ้ามีข้อมูลเก่า)
3. แก้ไข hooks ให้ใช้ Supabase แทน localStorage
