# ทดสอบระบบ Sign Up หลังปิด Email Verification

## 🧪 ขั้นตอนทดสอบ

### 1. ทดสอบสมัครสมาชิก

1. **เปิดเว็บแอป**: `http://localhost:5173`
2. **ไปที่หน้า Sign Up**
3. **กรอกข้อมูล**:
   - อีเมลใหม่ (ที่ยังไม่เคยใช้)
   - รหัสผ่าน (อย่างน้อย 6 ตัวอักษร)
   - ยืนยันรหัสผ่าน
4. **คลิก "สมัครสมาชิก"**

### 2. ตรวจสอบผลลัพธ์

**ควรเห็น:**
- ✅ ไม่มี error "Error sending confirmation email"
- ✅ ไม่มี error 500
- ✅ สมัครสมาชิกสำเร็จ
- ✅ เข้าสู่ระบบอัตโนมัติ (ไม่ต้องยืนยันอีเมล)

### 3. ตรวจสอบ Browser Console

**เปิด Developer Tools (F12) → Console:**

**ควรเห็น:**
- ✅ `Sign up successful`
- ✅ `Attempting sign up with email: ...`
- ❌ ไม่มี error สีแดง

**ถ้าเจอ error:**
- Copy error message ทั้งหมด
- Copy status code
- บอกว่า error เกิดที่บรรทัดไหน

### 4. ตรวจสอบ Supabase Dashboard

1. **ไปที่ Supabase Dashboard** → **Authentication** → **Users**
2. **ดูว่ามี user ใหม่ถูกสร้างหรือไม่**
3. **ตรวจสอบว่า user มี email verified หรือไม่**

## 📋 Checklist

- [ ] ทดสอบสมัครสมาชิก
- [ ] ไม่มี error ใน Browser Console
- [ ] สมัครสมาชิกสำเร็จ
- [ ] เข้าสู่ระบบอัตโนมัติ
- [ ] ตรวจสอบ Supabase Dashboard → Users

## 🆘 ถ้ายังเจอ Error

### Error: "Error sending confirmation email" (500)

**สาเหตุ:**
- Email verification อาจยังเปิดอยู่
- SMTP settings อาจไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบว่า "Confirm sign up" ปิดแล้วจริงๆ
2. ตรวจสอบ SMTP Settings ว่าถูกต้อง
3. ลองปิด Custom SMTP ชั่วคราว (ใช้ default)

### Error: "Invalid API key"

**สาเหตุ:**
- API Key ไม่ถูกต้อง
- Environment variables ไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบไฟล์ `app/.env`
2. ตรวจสอบว่า `VITE_SUPABASE_URL` และ `VITE_SUPABASE_ANON_KEY` ถูกต้อง
3. Restart dev server

### Error: "User already registered"

**สาเหตุ:**
- อีเมลนี้ถูกใช้แล้ว

**วิธีแก้:**
- ใช้อีเมลอื่น
- หรือ Login แทน Sign Up

## 💡 คำแนะนำ

**ถ้าทดสอบแล้วยังเจอ error:**
- บอก error message ที่เจอ
- บอก status code
- บอกว่าเกิดที่บรรทัดไหนใน Browser Console

**ไม่ต้องให้ API Secret Keys:**
- ใช้ Public Key (Anon Key) ที่มีอยู่แล้วตรวจสอบได้
- Secret Keys เป็นข้อมูลที่ละเอียดอ่อน ไม่ควรแชร์
