# ตรวจสอบและปิด Email Verification ให้ถูกต้อง

## 🔍 ปัญหา

ยังคงเจอ error: `Error sending confirmation email` (500) แม้ว่าจะตั้งค่า SMTP แล้ว

## ✅ วิธีแก้ไข (ทำตามขั้นตอนนี้)

### ขั้นตอนที่ 1: ปิด "Confirm sign up" ใน Templates Tab

1. **ไปที่ Supabase Dashboard** → **Authentication** → **Email** → **Templates** tab
2. **คลิกที่ "Confirm sign up"** (แถวแรก)
3. **ปิด toggle** "Enable email" หรือ **uncheck** checkbox
4. **Save** หรือ **Update**

### ขั้นตอนที่ 2: ตรวจสอบ SMTP Settings

1. **ไปที่ SMTP Settings tab**
2. **ตรวจสอบว่า:**
   - ✅ "Enable custom SMTP" **เปิดอยู่**
   - ✅ Host: `smtp.resend.com`
   - ✅ Port: `587`
   - ✅ Username: `resend`
   - ✅ Password: [Resend API Key]
   - ✅ Sender email: `onboarding@resend.dev`
3. **คลิก "Save changes"** (แม้ว่าจะตั้งค่าแล้ว)

### ขั้นตอนที่ 3: ทดสอบ SMTP Connection

1. **ใน SMTP Settings page** หาปุ่ม **"Test email"** หรือ **"Send test email"**
2. **คลิกเพื่อทดสอบ** ว่าส่ง email ได้หรือไม่
3. **ตรวจสอบ Resend Dashboard** → Logs ว่า email ถูกส่งหรือไม่

### ขั้นตอนที่ 4: Clear Browser Cache

1. **Hard refresh**: กด `Ctrl + Shift + R` (Windows) หรือ `Cmd + Shift + R` (Mac)
2. หรือ **Clear cache** ใน Browser

### ขั้นตอนที่ 5: ทดสอบสมัครสมาชิกใหม่

1. **ลองสมัครสมาชิกใหม่**
2. **ตรวจสอบ Browser Console** ว่าไม่มี error

## 🆘 ถ้ายังไม่ได้

### ตรวจสอบ Resend Dashboard

1. **ไปที่**: https://resend.com → Dashboard
2. **ดู Logs** หรือ **Emails**
3. **ตรวจสอบว่า:**
   - มี email ถูกส่งหรือไม่
   - มี error หรือไม่
   - API Key ยังใช้งานได้หรือไม่

### ลองสร้าง Resend API Key ใหม่

1. **Resend Dashboard** → **API Keys**
2. **สร้าง API Key ใหม่**
3. **Copy API Key ใหม่**
4. **ไปที่ Supabase** → **SMTP Settings**
5. **ใส่ API Key ใหม่** ใน Password field
6. **Save changes**

### ตรวจสอบ Supabase Logs

1. **Supabase Dashboard** → **Authentication** → **Audit Logs**
2. **ดู signup attempts** และ errors
3. **ตรวจสอบว่า** มี error อะไรเพิ่มเติมหรือไม่

## 📋 Checklist

- [ ] ปิด "Confirm sign up" ใน Templates tab
- [ ] ตรวจสอบ SMTP Settings ว่าถูกต้อง
- [ ] คลิก "Save changes" ใน SMTP Settings
- [ ] ทดสอบส่ง test email (ถ้ามี)
- [ ] Clear browser cache
- [ ] ทดสอบสมัครสมาชิกใหม่
- [ ] ตรวจสอบ Resend Dashboard → Logs
- [ ] ตรวจสอบ Browser Console ว่าไม่มี error

## 💡 สิ่งสำคัญ

**ต้องปิด "Confirm sign up" ใน Templates tab** ไม่ใช่แค่ปิดใน Settings!
