# แก้ไขปัญหา Email Validation

## 🔍 ปัญหาที่พบ

จาก Browser Console:
```
AuthApiError: Email address "123@gmail.com" is invalid
Status: 400 (Bad Request)
```

## ✅ สิ่งที่แก้ไขแล้ว

1. ✅ เพิ่ม Email Validation ใน Frontend
2. ✅ Clean email (trim + lowercase) ก่อนส่งไป Supabase
3. ✅ แสดง Error Message ที่ชัดเจนขึ้น

## 📝 วิธีแก้ไขปัญหา

### 1. ตรวจสอบ Supabase Email Settings

1. ไปที่ Supabase Dashboard → **Authentication** → **Providers** → **Email**
2. ตรวจสอบว่า:
   - **Enable Email provider** เปิดอยู่
   - **Confirm email** ปิดอยู่ (สำหรับทดสอบ)
3. ตรวจสอบ **Email Templates**:
   - ไปที่ **Authentication** → **Email Templates**
   - ตรวจสอบว่า templates ไม่มีปัญหา

### 2. ลองใช้อีเมลที่ถูกต้อง

**อีเมลที่ควรใช้:**
- ✅ `test@example.com`
- ✅ `yourname@gmail.com`
- ✅ `user@domain.com`

**อีเมลที่อาจมีปัญหา:**
- ⚠️ `123@gmail.com` (อาจถูก reject โดย Supabase)
- ⚠️ อีเมลที่ไม่มี domain จริง

### 3. ตรวจสอบ Browser Console

ดู error message ที่ชัดเจนขึ้น:
- ถ้าเห็น "รูปแบบอีเมลไม่ถูกต้อง" = Frontend validation
- ถ้าเห็น "Email address ... is invalid" = Supabase validation

## 🔧 การแก้ไขที่ทำแล้ว

### 1. เพิ่ม Email Validation
```typescript
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
if (!emailRegex.test(email)) {
  toast.error('รูปแบบอีเมลไม่ถูกต้อง');
  return;
}
```

### 2. Clean Email ก่อนส่ง
```typescript
const cleanEmail = email.trim().toLowerCase();
```

### 3. Error Handling ที่ดีขึ้น
- แสดง error message ที่ชัดเจน
- แยก error types

## 🧪 ทดสอบ

ลองใช้อีเมลเหล่านี้:
1. `test@example.com` - ควรทำงานได้
2. `yourname@gmail.com` - ควรทำงานได้
3. `123@gmail.com` - อาจมีปัญหา (ลองดู)

## ⚠️ ถ้ายังไม่ได้

1. **ตรวจสอบ Supabase Dashboard:**
   - Authentication → Logs
   - ดู error details

2. **ลองใช้ Legacy Anon Key:**
   - ไปที่ Settings → API → Legacy anon, service_role API keys
   - Copy anon public key
   - แก้ไข `.env`

3. **ตรวจสอบ Email Provider Settings:**
   - ตรวจสอบว่าเปิดใช้งานแล้ว
   - ตรวจสอบว่าไม่มี restrictions
